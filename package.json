{
  "dependencies": {
    "@project-serum/anchor": "^0.25.0",
    "dotenv": "^16.0.2",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^9.0.3"
  },
  "scripts": {
    "start": "node index.js",
    "build": "cd app && npm install && npm run build"
  }
}
